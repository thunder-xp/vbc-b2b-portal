import { BookOpen } from "lucide-react";
import { getKnowledgeLandingAction,searchKnowledgeAction } from "@/src/modules/knowledge-base/actions";
import { KnowledgeLandingView } from "@/src/modules/knowledge-base/landing-components";
export default async function KnowledgePage({searchParams}:{searchParams:Promise<{q?:string}>}){const query=(await searchParams).q?.trim()??"";const[landing,search]=await Promise.all([getKnowledgeLandingAction(),query?searchKnowledgeAction(query):Promise.resolve(null)]);if(!landing.success||!landing.data)return <p className="border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">База знаний временно недоступна.</p>;return <div className="space-y-6"><header><p className="flex items-center gap-2 text-xs font-semibold uppercase text-emerald-700"><BookOpen className="size-4"/>Самообслуживание</p><h1 className="mt-2 text-2xl font-semibold">База знаний</h1><p className="mt-2 text-sm text-zinc-600">Инструкции по работе с платформой, заказами, сервисом и поддержкой.</p></header><KnowledgeLandingView data={landing.data} query={query} results={search?.success?search.data:[]}/></div>}
