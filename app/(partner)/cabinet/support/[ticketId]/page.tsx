import Link from "next/link";
import { notFound } from "next/navigation";
import { getSupportTicketAction, PartnerSupportActions, SupportAttachmentUpload, SupportReplyForm, SupportTicketSummary } from "@/src/modules/partner-support";
export default async function SupportDetailPage({ params, searchParams }: { params: Promise<{ ticketId: string }>; searchParams: Promise<{ created?: string; attachment?: string }> }) { const { ticketId } = await params; const query = await searchParams; const result = await getSupportTicketAction(ticketId); if (!result.success || !result.data) notFound(); const detail = result.data; const immutable = ["closed", "rejected", "cancelled"].includes(detail.status); return <div className="mx-auto max-w-5xl space-y-6"><header><Link className="text-sm font-medium text-emerald-700" href="/cabinet/support">← Все заявки</Link><p className="mt-4 text-xs font-semibold uppercase text-emerald-700">IT-поддержка</p><h1 className="mt-1 text-2xl font-semibold">{detail.ticketNumber}</h1>{query.created === "1" ? <p aria-live="polite" className="mt-3 rounded-md border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800">Заявка создана. Номер сохранён в вашем кабинете.</p> : null}{query.attachment === "failed" ? <p aria-live="polite" className="mt-3 rounded-md border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900">Заявка сохранена, но файл не загрузился. Добавьте его ниже повторно.</p> : null}</header><SupportTicketSummary detail={detail} /><PartnerSupportActions detail={detail} />{!immutable ? <section className="grid gap-8 border-t border-zinc-200 pt-6 md:grid-cols-2"><div><h2 className="text-lg font-semibold">Ответить</h2><div className="mt-3"><SupportReplyForm detail={detail} /></div></div><div><h2 className="text-lg font-semibold">Добавить материал</h2><div className="mt-3"><SupportAttachmentUpload ticketId={detail.id} /></div></div></section> : null}</div>; }
