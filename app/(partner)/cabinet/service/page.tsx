import Link from "next/link";
import { listServiceCasesAction,ServiceCaseList,SERVICE_STATUSES,SERVICE_STATUS_LABELS } from "@/src/modules/service-center";
import { PartnerWarrantySerialLookup } from "@/src/modules/warranty-serials";
export default async function ServicePage({searchParams}:{searchParams:Promise<{query?:string;status?:string;page?:string}>}){const params=await searchParams;const result=await listServiceCasesAction(params);return <div className="mx-auto max-w-6xl space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-semibold uppercase text-emerald-700">Сервис Novotech</p><h1 className="mt-1 text-2xl font-semibold">Сервис и гарантия</h1><p className="mt-2 text-sm text-zinc-600">Регистрация обращений, диагностика и сервисные документы.</p></div><Link className="inline-flex min-h-11 items-center justify-center rounded-md bg-emerald-700 px-4 text-sm font-semibold text-white" href="/cabinet/service/new">Создать заявку</Link></header><PartnerWarrantySerialLookup/><form className="grid gap-2 sm:grid-cols-[minmax(0,1fr)_220px_auto]"><input className="min-h-11 rounded-md border border-zinc-300 px-3 text-sm" defaultValue={params.query} name="query" placeholder="Номер, товар или серийный номер"/><select className="min-h-11 rounded-md border border-zinc-300 px-3 text-sm" defaultValue={params.status??""} name="status"><option value="">Все статусы</option>{SERVICE_STATUSES.map(value=><option key={value} value={value}>{SERVICE_STATUS_LABELS[value]}</option>)}</select><button className="min-h-11 rounded-md border border-zinc-300 px-4 text-sm font-semibold">Найти</button></form>{result.success?<ServiceCaseList page={result.data}/>:<p className="rounded-md border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">Не удалось загрузить заявки. Повторите попытку позже.</p>}</div>}
