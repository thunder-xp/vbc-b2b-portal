import Link from "next/link";
import { notFound } from "next/navigation";
import { getPurchasingListAction } from "@/src/modules/purchasing-lists/actions";
import { PurchasingListActions, PurchasingListEditor } from "@/src/modules/purchasing-lists/components";

export default async function PurchasingListDetailPage({ params }: { params: Promise<{ listId: string }> }) { const { listId } = await params; const result = await getPurchasingListAction(listId); if (!result.success) { if (result.errorCode === "NOT_FOUND") notFound(); return <p className="rounded-md border border-rose-200 bg-rose-50 p-5 text-sm text-rose-800">Список закупок недоступен.</p>; } return <div className="mx-auto max-w-7xl space-y-5"><header><Link className="text-sm font-semibold text-emerald-700" href="/cabinet/purchasing-lists">← Списки закупок</Link><div className="mt-2 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><h1 className="text-2xl font-semibold">{result.data.name}</h1>{result.data.archivedAt ? <span className="rounded bg-zinc-200 px-2 py-1 text-xs font-semibold">Архив</span> : null}</div><PurchasingListActions archived={Boolean(result.data.archivedAt)} canManage={result.data.canManage} listId={result.data.id} name={result.data.name} revision={result.data.revision} /></div><p className="mt-1 text-sm text-zinc-500">Текущие цены и наличие загружаются из локальных коммерческих витрин.</p></header><PurchasingListEditor initial={result.data} /></div>; }
