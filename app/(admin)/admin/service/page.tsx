import { listAdminServiceCasesAction,ServiceCaseList,SERVICE_STATUSES,SERVICE_STATUS_LABELS } from "@/src/modules/service-center";
export default async function AdminServicePage({searchParams}:{searchParams:Promise<{query?:string;status?:string;page?:string}>}){const params=await searchParams;const result=await listAdminServiceCasesAction(params);return <div className="space-y-6"><header><p className="text-xs font-semibold uppercase text-emerald-700">Операции</p><h1 className="mt-1 text-2xl font-semibold">Сервисные заявки</h1><p className="mt-2 text-sm text-zinc-600">Диагностика, ремонт, замена и возврат оборудования партнёров.</p></header><form className="grid gap-2 sm:grid-cols-[minmax(0,1fr)_220px_auto]"><input className="min-h-11 rounded-md border border-zinc-300 px-3 text-sm" defaultValue={params.query} name="query" placeholder="Заявка, компания, товар, серийный номер"/><select className="min-h-11 rounded-md border border-zinc-300 px-3 text-sm" defaultValue={params.status??""} name="status"><option value="">Все статусы</option>{SERVICE_STATUSES.map(value=><option key={value} value={value}>{SERVICE_STATUS_LABELS[value]}</option>)}</select><button className="min-h-11 rounded-md bg-zinc-900 px-4 text-sm font-semibold text-white">Найти</button></form>{result.success?<ServiceCaseList admin page={result.data}/>:<p className="rounded-md border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">Не удалось загрузить заявки.</p>}</div>}
